An intuitive, easy to use Python based command line Arch Linux installer.

Q. How can I use this?

A. Boot into your Arch ISO, connect to the internet if you haven't already to run `pacman -Sy git && git clone https://github.com/itsbt125/test-installer-build && cd test-installer-build && python main.py`
